/** next.config.js */
const nextConfig = {
  // ...existing config...
  images: {
    // Add any external domains you use (e.g. picsum, unsplash)
    domains: ["images.unsplash.com", "picsum.photos"],
  },
};

module.exports = nextConfig;
